name: Release
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    # ubuntu-22.04 (not -latest/24.04): 24.04's AppArmor unprivileged-userns
    # restriction breaks linuxdeploy/appimagetool during AppImage bundling.
    # 22.04 also links an older glibc → more portable AppImage.
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Assert tag matches config version
        if: startsWith(github.ref, 'refs/tags/v')
        run: ./scripts/check-version.sh "${GITHUB_REF_NAME}"

      - name: Install system build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev \
            libudev-dev \
            build-essential \
            libfuse2 \
            file

      - uses: dtolnay/rust-toolchain@stable
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install JS deps (root + frontend)
        run: |
          pnpm install --frozen-lockfile
          pnpm --dir frontend install --frozen-lockfile

      - name: Stage asm-cli sidecar
        run: ./scripts/stage-sidecar.sh

      - name: Build, sign, and publish bundles
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""
          # linuxdeploy/appimagetool are AppImages needing FUSE (absent on the
          # runner) → run them extract-and-run; and skip linuxdeploy's strip
          # step, which fails on the bundled binaries (reproduced locally).
          APPIMAGE_EXTRACT_AND_RUN: "1"
          NO_STRIP: "true"
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Arctis Sound Manager ${{ github.ref_name }}"
          releaseBody: "See the assets below. The AppImage self-updates via the in-app banner."
          releaseDraft: false
          prerelease: false
